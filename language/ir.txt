Identifiers
% local identifiers
@ global identifiers

High level structure:
program == modules
module == functions + global varaibles + symbol table

global funciton has linkage type
internal
private
external

calling conventions
how parameter passed and how return value is returned
